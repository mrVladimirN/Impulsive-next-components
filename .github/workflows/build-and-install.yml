name: Build and Install

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env: # Define environment variables for the entire job
      FIREBASE_PROJECT_ID: ${{ secrets.Private_Firebase.PROJECT_ID }}
      FIREBASE_PRIVATE_KEY: ${{ secrets.Private_Firebase.PRIVATE_KEY }}
      FIREBASE_CLIENT_EMAIL: ${{ secrets.Private_Firebase.CLIENT_EMAIL }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build:ts
